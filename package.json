{
  "name": "r102",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "dist": "for f in $(git ls-files '*.html'); do bun build $f --outdir dist/$(dirname -- $f) --experimental-html --experimental-css --minify --production; done"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
